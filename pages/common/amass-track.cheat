; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/amass-track.md

% amass-track, common

# Show the difference between the last two enumerations of the specified domain
amass track -dir <path_to_database_directory> -d <domain_name> -last 2

# Show the difference between a certain point in time and the last enumeration
amass track -dir <path_to_database_directory> -d <domain_name> -since <01_02 15:04:05 2006 MST>
