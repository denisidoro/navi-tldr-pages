; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/ansible-galaxy.md

% ansible-galaxy, common

# Install a role
ansible-galaxy install <username_role_name>

# Remove a role
ansible-galaxy remove <username_role_name>

# List installed roles
ansible-galaxy list

# Search for a given role
ansible-galaxy search <role_name>

# Create a new role
ansible-galaxy init <role_name>
