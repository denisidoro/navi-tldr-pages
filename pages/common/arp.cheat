; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/arp.md

% arp, common

# Show the current ARP table
arp -a

# Delete a specific entry
arp -d <address>

# Create an entry in the ARP table
arp -s <address> <mac_address>
