; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/base32.md

% base32, common

# Encode a file
base32 <path_to_file>

# Wrap encoded output at a specific width (`0` disables wrapping)
base32 <-w|--wrap> <0|76|...> <path_to_file>

# Decode a file
base32 <-d|--decode> <path_to_file>

# Encode from `stdin`
<command> | base32

# Decode from `stdin`
<command> | base32 <-d|--decode>
