; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/bazel.md

% bazel, common

# Build the specified target in the workspace
bazel build <target>

# Remove output files and stop the server if running
bazel clean

# Stop the bazel server
bazel shutdown

# Display runtime info about the bazel server
bazel info

# Display help
bazel help

# Display version
bazel version
