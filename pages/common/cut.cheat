; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/cut.md

% cut, common

# Print a specific character/field range of each line
<command> | cut --<characters_fields>=<1_1_10_1_10_1___10>

# Print a range of each line with a specific delimiter
<command> | cut --delimiter="<_>" --<characters>=<1>

# Print a range of each line of the specific file
cut --<characters>=<1> <path_to_file>
