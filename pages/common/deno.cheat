; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/deno.md

% deno, common

# Run a JavaScript or TypeScript file
deno run <path_to_file_ts>

# Start a REPL (interactive shell)
deno

# Run a file with network access enabled
deno run --allow-net <path_to_file_ts>

# Run a file from a URL
deno run <https___deno_land_std_examples_welcome_ts>

# Install an executable script from a URL
deno install <https___deno_land_std_examples_colors_ts>
