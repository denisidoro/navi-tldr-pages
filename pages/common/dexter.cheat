; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/dexter.md

% dexter, common

# Create and authenticate a user with Google OIDC
dexter auth -i <client_id> -s <client_secret>

# Override the default kube config location
dexter auth -i <client_id> -s <client_secret> --kube-config <sample_config>
