; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/esptool.py.md

% esptool.py, common

# Flash a firmware file to an ESP chip with a given port and baud rate
sudo esptool.py --port <port> --baud <baud_rate> write_flash 0x0 <path_to_firmware.bin>

# Clear the flash of an ESP chip
sudo esptool.py --port <port> --baud <baud_rate> erase_flash
