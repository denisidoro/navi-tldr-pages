; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/fusermount.md

% fusermount, common

# Unmount a FUSE filesystem
fusermount -u <path_to_mount_point>

# Unmount a FUSE filesystem as soon as it becomes unused
fusermount -z <path_to_mount_point>

# Display version
fusermount --version
