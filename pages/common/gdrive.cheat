; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/gdrive.md

% gdrive, common

# Upload a local path to the parent folder with the specified ID
gdrive upload -p <id> <path_to_file_or_folder>

# Download file or directory by ID to current directory
gdrive download <id>

# Download to a given local path by its ID
gdrive download --path <path_to_folder> <id>

# Create a new revision of an ID using a given file or folder
gdrive update <id> <path_to_file_or_folder>
