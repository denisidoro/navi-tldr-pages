; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/git-merge-repo.md

% git-merge-repo, common

# Merge a repository's branch into the current repository's directory
git merge-repo <path_to_repo> <branch_name> <path_to_directory>

# Merge a remote repository's branch into the current repository's directory, not preserving history
git merge-repo <path_to_remote_repo> <branch_name> .
