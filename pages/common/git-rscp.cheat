; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/git-rscp.md

% git-rscp, common

# Copy specific files from a remote
git rscp <remote_name> <path_to_file1 path_to_file2 ...>

# Copy a specific directory from a remote
git rscp <remote_name> <path_to_directory>
