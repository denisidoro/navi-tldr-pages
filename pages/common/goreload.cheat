; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/goreload.md

% goreload, common

# Set the name of the binary file to watch (defaults to `.goreload`)
goreload -b <path_to_binary> <path_to_file>.go

# Set a custom log prefix (defaults to `goreload`)
goreload --logPrefix <prefix> <path_to_file>.go

# Reload whenever any file changes
goreload --all
