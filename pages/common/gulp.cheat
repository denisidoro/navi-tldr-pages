; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/gulp.md

% gulp, common

# Run the default task
gulp

# Run individual tasks
gulp <task> <othertask>

# Print the task dependency tree for the loaded gulpfile
gulp --tasks
