; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/gunzip.md

% gunzip, common

# Extract a file from an archive, replacing the original file if it exists
gunzip <archive_tar_gz>

# Extract a file to a target destination
gunzip --stdout <archive_tar_gz> > <archive_tar>

# Extract a file and keep the archive file
gunzip --keep <archive_tar_gz>

# List the contents of a compressed file
gunzip --list <file_txt_gz>
