; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/ioping.md

% ioping, common

# Show disk I_O latency using the default values and the current directory
ioping .

# Measure latency on _tmp using 10 requests of 1 megabyte each
ioping -c 10 -s 1M _tmp

# Measure disk seek rate on `_dev_sdX`
ioping -R <_dev_sdX>

# Measure disk sequential speed on `_dev_sdX`
ioping -RL <_dev_sdX>
