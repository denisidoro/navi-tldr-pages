; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/javac.md

% javac, common

# Compile a `.java` file
javac <path_to_file.java>

# Compile several `.java` files
javac <path_to_file1.java path_to_file2.java ...>

# Compile all `.java` files in current directory
javac <*.java>

# Compile a `.java` file and place the resulting class file in a specific directory
javac -d <path_to_directory> <path_to_file.java>
