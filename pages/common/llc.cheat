; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/llc.md

% llc, common

# Compile a bitcode or IR file to an assembly file with the same base name
llc <path_to_file.ll>

# Enable all optimizations
llc -O3 <path_to_input.ll>

# Output assembly to a specific file
llc --output <path_to_output.s>

# Emit fully relocatable, position independent code
llc -relocation-model=pic <path_to_input.ll>
