; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/ninja.md

% ninja, common

# Build in the current directory
ninja

# Build a program in a given directory
ninja -C <path_to_directory>

# Show targets (e.g. `install` and `uninstall`)
ninja -t targets

# Show help
ninja -h
