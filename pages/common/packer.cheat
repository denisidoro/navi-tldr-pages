; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/packer.md

% packer, common

# Build an image
packer build <path_to_config.json>

# Check the syntax of a Packer image config
packer validate <path_to_config.json>

# Format a Packer image config
packer fmt <path_to_config.pkr.hcl>
