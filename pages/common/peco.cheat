; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/peco.md

% peco, common

# Start `peco` on all files in the specified directory
find <path_to_directory> -type f | peco

# Start `peco` for running processes
ps aux | peco

# Start `peco` with a specified query
peco --query "<query>"
