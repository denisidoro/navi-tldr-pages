; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/pop.md

% pop, common

# Launch the Text-based User Interface
pop

# Send an email using the content of a Markdown file as body
pop < <path_to_message_md> --from <me_example_com> --to <you_example_com> --subject "{{On the Subject of Ducks...}}" --attach <path_to_attachment>

# Display help and exit
pop --help
