; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/pyats.md

% pyats, common

# Run a `pyATS` subcommand
pyats <subcommand>

# Display version
pyats version check

# Display help
pyats --help

# Display help about a specific subcommand
pyats <subcommand> --help
