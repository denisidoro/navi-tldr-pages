; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/ranger.md

% ranger, common

# Launch ranger
ranger

# Show only directories
ranger --show-only-dirs

# Change the configuration directory
ranger --confdir=<path_to_directory>

# Change the data directory
ranger --datadir=<path_to_directory>

# Print CPU usage statistics on exit
ranger --profile
