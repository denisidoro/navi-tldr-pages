; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/shards.md

% shards, common

# Create a skeleton `shard.yml` file
shards init

# Install dependencies from a `shard.yml` file
shards install

# Update all dependencies
shards update

# List all installed dependencies
shards list

# List version of dependency
shards version <path_to_dependency_directory>
