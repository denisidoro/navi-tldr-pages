; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/subfinder.md

% subfinder, common

# Find subdomains for a specific domain
subfinder -d <example_com>

# Show only the subdomains found
subfinder --silent -d <example_com>

# Use bruteforcing to find subdomains
subfinder -d <example_com> -b

# Remove wildcard subdomains
subfinder -nW -d <example_com>

# Use a given comma-separated list of resolvers
subfinder -r <8_8_8_8>,<1_1_1_1> -d <example_com>
