; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/wpaclean.md

% wpaclean, common

# Clean capture and save only the 4-way handshake and a beacon in the result
wpaclean <path_to_result_cap> <path_to_capture_cap>

# Clean multiple captures and save 4-way handshakes and beacons in the result
wpaclean <path_to_result_cap> {{path/to/capture1.cap path/to/capture2.cap ...}}
