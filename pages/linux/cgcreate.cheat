; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/cgcreate.md

% cgcreate, linux

# Create a new group
cgcreate -g <group_type>:<group_name>

# Create a new group with multiple cgroup types
cgcreate -g <group_type1>,<group_type2>:<group_name>

# Create a subgroup
mkdir _sys_fs_cgroup_<group_type>_<group_name>_<subgroup_name>
