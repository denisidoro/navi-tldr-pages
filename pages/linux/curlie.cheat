; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/curlie.md

% curlie, linux

# Send a GET request
curlie <httpbin.org_get>

# Send a POST request
curlie post <httpbin.org_post> <name=john> <age:=25>

# Send a GET request with query parameters (e.g. `first_param=5&second_param=true`)
curlie get <httpbin.org_get> <first_param==5> <second_param==true>

# Send a GET request with a custom header
curlie get <httpbin.org_get> <header-name:header-value>
