; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/eww.md

% eww, linux

# Start the daemon
eww daemon

# Open a widget
eww -c <path_to_source_code_directory> open <window_name>

# Close a widget
eww -c <path_to_source_code_directory> close <window_name>

# Reload the configuration
eww reload

# Kill the daemon
eww kill

# Print and watch logs
eww logs
