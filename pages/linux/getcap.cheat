; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/getcap.md

% getcap, linux

# Get capabilities for the given files
getcap <path_to_file1 path_to_file2 ...>

# Get capabilities for all the files recursively under the given directories
getcap -r <path_to_directory1 path_to_directory2 ...>

# Displays all searched entries even if no capabilities are set
getcap -v <path_to_file1 path_to_file2 ...>
