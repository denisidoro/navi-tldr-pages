; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/isoinfo.md

% isoinfo, linux

# List all the files included in an ISO image
isoinfo -f -i <path_to_image.iso>

# E[x]tract a specific file from an ISO image and send it out `stdout`
isoinfo -i <path_to_image.iso> -x <PATH_TO_FILE_INSIDE_ISO.EXT>

# Show header information for an ISO disk image
isoinfo -d -i <path_to_image.iso>
