; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/megatools-dl.md

% megatools-dl, linux

# Download files from a `mega.nz` link into the current directory
megatools-dl <https:__mega.nz...>

# Download files from a `mega.nz` link into a specific directory
megatools-dl --path <path_to_directory> <https:__mega.nz...>

# Interactively choose which files to download
megatools-dl --choose-files <https:__mega.nz...>

# Limit the download speed in KiB_s
megatools-dl --limit-speed <speed> <https:__mega.nz...>
