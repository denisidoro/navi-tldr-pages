; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/mkfs.xfs.md

% mkfs.xfs, linux

# Create an XFS filesystem inside partition 1 on a device (`X`)
sudo mkfs.xfs <dev_sdX1>

# Create an XFS filesystem with a volume label
sudo mkfs.xfs -L <volume_label> <dev_sdX1>
