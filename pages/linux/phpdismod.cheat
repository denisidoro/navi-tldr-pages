; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/phpdismod.md

% phpdismod, linux

# Disable the json extension for every SAPI of every PHP version
sudo phpdismod <json>

# Disable the json extension for PHP 7.3 with the cli SAPI
sudo phpdismod -v <7_3> -s <cli> <json>
