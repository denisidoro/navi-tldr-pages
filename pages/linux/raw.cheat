; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/raw.md

% raw, linux

# Bind a raw character device to a block device
raw _dev_raw_raw<1> <_dev_block_device>

# Query an existing binding instead of setting a new one
raw _dev_raw_raw<1>

# Query all bound raw devices
raw -qa
