; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/startx.md

% startx, linux

# Start an X session
startx

# Start an X session with a predefined depth value
startx -- -depth <value>

# Start an X session with a predefined dpi value
startx -- -dpi <value>

# Override the settings in the `.xinitrc` file and start a new X session
startx /<path_to_window_manager_or_desktop_environment>
