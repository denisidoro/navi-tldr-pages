; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/toolbox.md

% toolbox, linux

# Run a `toolbox` subcommand
toolbox <subcommand>

# Display help for a specific subcommand (such as `create`, `enter`, `rm`, `rmi`, etc.)
toolbox help <subcommand>

# Display help
toolbox --help

# Display version
toolbox --version
