; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/utmpdump.md

% utmpdump, linux

# Dump the `_var_log_wtmp` file to the standard output as plain text
utmpdump <_var_log_wtmp>

# Load a previously dumped file into `_var_log_wtmp`
utmpdump -r <dumpfile> > <_var_log_wtmp>
