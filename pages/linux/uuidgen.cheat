; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/uuidgen.md

% uuidgen, linux

# Create a random UUID
uuidgen --random

# Create a UUID based on the current time
uuidgen --time

# Create a UUID based on the hash of a URL
uuidgen --sha1 --namespace <_url> --name <object_name>
