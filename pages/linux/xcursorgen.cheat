; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/xcursorgen.md

% xcursorgen, linux

# Create an X cursor file using a configuration file
xcursorgen <path_to_config.cursor> <path_to_output_file>

# Create an X cursor file using a configuration file and specify the path to the image files
xcursorgen --prefix <path_to_image_directory_> <path_to_config.cursor> <path_to_output_file>

# Create an X cursor file using a configuration file and write the output to `stdout`
xcursorgen <path_to_config.cursor>
