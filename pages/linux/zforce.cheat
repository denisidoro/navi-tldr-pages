; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/zforce.md

% zforce, linux

# Add a `.gz` extension to the supplied Gzip files (Note: other files are ignored)
zforce <path_to_file1 path_to_file2 ...>
