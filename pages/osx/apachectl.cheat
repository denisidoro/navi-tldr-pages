; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/osx/apachectl.md

% apachectl, osx

# Start the `org.apache.httpd` launchd job
apachectl start

# Stop the launchd job
apachectl stop

# Stop, then start launchd job
apachectl restart
