; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/osx/diskutil.md

% diskutil, osx

# List all currently available disks, partitions and mounted volumes
diskutil list

# Repair the filesystem data structures of a volume
diskutil repairVolume <dev_disk_device>

# Unmount a volume
diskutil unmountDisk <dev_disk_device>

# Eject a CD/DVD (unmount first)
diskutil eject <dev_disk_device1>
