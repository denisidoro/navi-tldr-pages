; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/osx/eval.md

% eval, osx

# Call `echo` with the "foo" argument
eval "{{echo foo}}"

# Set a variable in the current shell
eval "<foo_bar>"
