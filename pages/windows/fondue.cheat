; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/windows/fondue.md

% fondue, windows

# Enable a specific Windows feature
fondue _enable-feature:<feature>

# Hide all output messages to the user
fondue _enable-feature:<feature> _hide-ux:all

# Specify a caller process name for error reporting
fondue _enable-feature:<feature> _caller-name:<name>
